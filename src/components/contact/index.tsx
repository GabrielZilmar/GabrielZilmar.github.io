import ContainerLayout from "~/components/layout/Container/Container";

const Contact: React.FC = () => {
  return <ContainerLayout id="contact">Contact</ContainerLayout>;
};

export default Contact;
